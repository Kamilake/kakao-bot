import { ObjectMapperBase, ArrayMapper } from "./common";
export declare namespace Serializer {
    function serialize<T extends object>(obj: T[], mapper: ArrayMapper): any[];
    function serialize<T extends object>(obj: T, mapper: ObjectMapperBase): any;
    function deserialize<T extends object>(rawObj: any[], mapper: ArrayMapper): T[];
    function deserialize<T extends object>(rawObj: any, mapper: ObjectMapperBase): T;
}
