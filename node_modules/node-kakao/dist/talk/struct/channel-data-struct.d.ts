import { ChannelType } from "../channel/channel-type";
import { StructBase } from "./struct-base";
import { Long } from "bson";
import { ObjectMapper } from "json-proxy-mapper";
import { ChannelClientMetaStruct } from "./channel-meta-struct";
export interface ChannelDataStruct extends StructBase {
    channelId: Long;
    type: ChannelType;
    memberCount: number;
    pushAlert: boolean;
    linkId?: Long;
    openToken?: number;
    metadata?: ChannelClientMetaStruct;
}
export declare namespace ChannelDataStruct {
    const Mappings: {
        channelId: string;
        type: string;
        memberCount: string;
        pushAlert: string;
        linkId: string;
        openToken: string;
        metadata: string;
    };
    const MAPPER: ObjectMapper;
}
