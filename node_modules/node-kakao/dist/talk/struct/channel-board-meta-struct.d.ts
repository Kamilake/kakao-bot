import { StructBase } from "./struct-base";
import { ObjectMapper } from "json-proxy-mapper";
import { Long } from "bson";
export interface ChannelBoardMetaStruct extends StructBase {
    type: ChannelBoardMetaType;
    content: string;
    boardRevision: Long;
    userRevision: Long;
}
export declare namespace ChannelBoardMetaStruct {
    const Mappings: {
        type: string;
        content: string;
        boardRevision: string;
        userRevision: string;
    };
    const MAPPER: ObjectMapper;
}
export declare enum ChannelBoardMetaType {
    NONE = 0,
    FLOATING_NOTICE = 1,
    SIDE_NOTICE = 2,
    BADGE = 3
}
