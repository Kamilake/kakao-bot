import { Long } from "bson";
import { ChatlogStruct } from "./chatlog-struct";
import { DisplayMemberStruct } from "./member-struct";
import { ChannelMetaStruct } from "./channel-meta-struct";
import { ObjectMapper, Converter } from "json-proxy-mapper";
import { ChannelDataStruct } from "./channel-data-struct";
export interface ChannelInfoStruct extends ChannelDataStruct {
    activeMemberCount: number;
    newMessageCount: number;
    lastUpdatedAt: string;
    lastMessage: string;
    lastLogId: Long;
    lastSeenLogId: Long;
    lastChatLog?: ChatlogStruct;
    displayMemberList: DisplayMemberStruct[];
    channelMetaList?: ChannelMetaStruct[];
    isDirectChat: boolean;
}
export declare namespace ChannelInfoStruct {
    const Mappings: {
        channelId: string;
        type: string;
        activeMemberCount: string;
        newMessageCount: string;
        lastUpdatedAt: string;
        lastMessage: string;
        lastLogId: string;
        lastSeenLogId: string;
        lastChatLog: string;
        metadata: string;
        displayMemberList: string;
        pushAlert: string;
        channelMetaList: string;
        isDirectChat: string;
        linkId: string;
        openToken: string;
    };
    const ConvertMap: {
        displayMemberList: Converter.Array<object>;
        lastChatLog: Converter.Object<object>;
    };
    const MAPPER: ObjectMapper;
}
