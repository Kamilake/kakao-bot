import { StructBase } from "./struct-base";
import { ChatType } from "../chat/chat-type";
import { Long } from "bson";
import { ObjectMapper } from "json-proxy-mapper";
export declare enum ChatRefererType {
    UNKNOWN = 0,
    KAKAOI = 1,
    BOT = 2
}
export interface ChatlogStruct extends StructBase {
    logId: Long;
    prevLogId: Long;
    senderId: Long;
    channelId: Long;
    type: ChatType;
    text: string;
    sendTime: number;
    referer?: ChatRefererType;
    rawAttachment?: string;
    supplement?: string;
    messageId: number;
}
export declare namespace ChatlogStruct {
    const Mappings: {
        logId: string;
        prevLogId: string;
        senderId: string;
        channelId: string;
        type: string;
        text: string;
        sendTime: string;
        rawAttachment: string;
        messageId: string;
        referer: string;
        supplement: string;
    };
    const MAPPER: ObjectMapper;
}
