import { RequestHeader } from "./web-api-client";
import { ClientConfigProvider } from "../config/client-config-provider";
export interface ApiHeaderDecorator {
    fillHeader(header: RequestHeader): void;
}
export declare class BasicHeaderDecorator implements ApiHeaderDecorator {
    private configProvider;
    static readonly INSTANCE: BasicHeaderDecorator;
    constructor(configProvider: ClientConfigProvider);
    get UserAgent(): string;
    fillHeader(header: RequestHeader): void;
}
export declare class AHeaderDecorator implements ApiHeaderDecorator {
    private configProvider;
    static readonly INSTANCE: AHeaderDecorator;
    constructor(configProvider: ClientConfigProvider);
    fillHeader(header: RequestHeader): void;
}
export declare class CHeaderDecorator implements ApiHeaderDecorator {
    constructor();
    fillHeader(header: RequestHeader): void;
}
