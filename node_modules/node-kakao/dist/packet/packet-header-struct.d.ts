export interface PacketHeader {
    packetId: number;
    statusCode: number;
    packetName: string;
    bodyType: number;
    bodySize: number;
}
export interface EncryptedPacketHeader {
    encryptedSize: number;
}
