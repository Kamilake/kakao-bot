import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
import { Long } from "bson";
export declare class PacketAddMemberReq extends LocoBsonRequestPacket {
    ChannelId: Long;
    MemberIdList: Long[];
    constructor(ChannelId?: Long, MemberIdList?: Long[]);
    get PacketName(): string;
    toBodyJson(): {
        chatId: Long;
        memberIds: Long[];
    };
}
export declare class PacketAddMemberRes extends LocoBsonResponsePacket {
    ChannelId: Long;
    MemberIdList: Long[];
    constructor(status: number, ChannelId?: Long, MemberIdList?: Long[]);
    get PacketName(): string;
    readBodyJson(rawData: any): void;
}
