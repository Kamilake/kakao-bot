import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
import { Long } from "bson";
export declare class PacketKickLeaveReq extends LocoBsonRequestPacket {
    LinkId: Long;
    ChannelId: Long;
    constructor(LinkId?: Long, ChannelId?: Long);
    get PacketName(): string;
    toBodyJson(): {
        li: Long;
        c: Long;
    };
}
export declare class PacketKickLeaveRes extends LocoBsonResponsePacket {
    LastTokenId: Long;
    constructor(status: number, LastTokenId?: Long);
    get PacketName(): string;
    readBodyJson(rawBody: any): void;
}
