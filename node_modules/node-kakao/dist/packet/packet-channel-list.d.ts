import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
import { Long } from "bson";
import { ChannelDataStruct } from "../talk/struct/channel-data-struct";
export declare class PacketChannelListReq extends LocoBsonRequestPacket {
    ChannelIdList: Long[];
    MaxIdList: Long[];
    LastTokenId: Long;
    LastChatId: Long;
    constructor(ChannelIdList?: Long[], MaxIdList?: Long[], LastTokenId?: Long, LastChatId?: Long);
    get PacketName(): string;
    toBodyJson(): {
        chatIds: Long[];
        maxIds: Long[];
        lastTokenId: Long;
        lastChatId: Long;
    };
}
export declare class PacketChannelListRes extends LocoBsonResponsePacket {
    ChatDataList: ChannelDataStruct[];
    constructor(status: number, ChatDataList?: ChannelDataStruct[]);
    get PacketName(): string;
    readBodyJson(rawData: any): void;
}
