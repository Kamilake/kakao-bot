import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
import { Long } from "bson";
import { OpenKickedMemberStruct } from "../talk/struct/open/open-link-struct";
export declare class PacketKickListSyncReq extends LocoBsonRequestPacket {
    LinkId: Long;
    constructor(LinkId?: Long);
    get PacketName(): string;
    toBodyJson(): {
        li: Long;
    };
}
export declare class PacketKickListSyncRes extends LocoBsonResponsePacket {
    KickedMemberList: OpenKickedMemberStruct[];
    constructor(status: number, KickedMemberList?: OpenKickedMemberStruct[]);
    get PacketName(): string;
    readBodyJson(rawData: any): void;
}
