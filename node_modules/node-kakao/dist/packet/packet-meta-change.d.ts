import { LocoBsonResponsePacket } from "./loco-bson-packet";
import { Long } from "bson";
import { ChannelMetaStruct } from "../talk/struct/channel-meta-struct";
export declare class PacketMetaChangeRes extends LocoBsonResponsePacket {
    ChannelId: Long;
    Meta?: ChannelMetaStruct | undefined;
    constructor(status: number, ChannelId?: Long, Meta?: ChannelMetaStruct | undefined);
    get PacketName(): string;
    readBodyJson(rawData: any): void;
}
