import { Long } from "bson";
import { LocoBsonRequestPacket } from "../loco-bson-packet";
export declare abstract class MediaRequestBasePacket extends LocoBsonRequestPacket {
    UserId: Long;
    Os: string;
    Version: string;
    NetworkType: number;
    NetworkMccMnc: string;
    constructor(UserId?: Long, Os?: string, Version?: string, NetworkType?: number, NetworkMccMnc?: string);
    abstract get PacketName(): string;
    toBodyJson(): {
        u: Long;
        os: string;
        av: string;
        nt: number;
        mm: string;
    };
}
