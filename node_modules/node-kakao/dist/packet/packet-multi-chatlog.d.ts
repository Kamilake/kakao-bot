import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
import { Long } from "bson";
import { ChatlogStruct } from "../talk/struct/chatlog-struct";
export declare class PacketMultiChatlogReq extends LocoBsonRequestPacket {
    ChannelIdList: Long[];
    SinceList: Long[];
    constructor(ChannelIdList?: Long[], SinceList?: Long[]);
    get PacketName(): string;
    toBodyJson(): {
        chatIds: Long[];
        sinces: Long[];
    };
}
export declare class PacketMultiChatlogRes extends LocoBsonResponsePacket {
    ChatlogList: ChatlogStruct[];
    constructor(status: number, ChatlogList?: ChatlogStruct[]);
    get PacketName(): string;
    readBodyJson(rawData: any): void;
}
