import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
import { Long } from "bson";
export declare class PacketKickListDelItemReq extends LocoBsonRequestPacket {
    LinkId: Long;
    ChannelId: Long;
    MemberId: Long;
    constructor(LinkId?: Long, ChannelId?: Long, MemberId?: Long);
    get PacketName(): string;
    toBodyJson(): {
        li: Long;
        c: Long;
        kid: Long;
    };
}
export declare class PacketKickListDelItemRes extends LocoBsonResponsePacket {
    constructor(status: number);
    get PacketName(): string;
    readBodyJson(rawData: any): void;
}
