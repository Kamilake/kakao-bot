import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
import { Long } from "bson";
import { ChannelInfoStruct } from "../talk/struct/channel-info-struct";
export declare class PacketChannelInfoReq extends LocoBsonRequestPacket {
    ChannelId: Long;
    constructor(ChannelId?: Long);
    get PacketName(): string;
    toBodyJson(): {
        chatId: Long;
    };
}
export declare class PacketChannelInfoRes extends LocoBsonResponsePacket {
    ChatInfo?: ChannelInfoStruct | undefined;
    constructor(status: number, ChatInfo?: ChannelInfoStruct | undefined);
    get PacketName(): string;
    readBodyJson(rawJson: any): void;
}
