import { Long } from "bson";
import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
export declare class PacketCheckJoinReq extends LocoBsonRequestPacket {
    LinkId: Long;
    Passcode: string;
    constructor(LinkId?: Long, Passcode?: string);
    get PacketName(): string;
    toBodyJson(): {
        li: Long;
        pc: string;
    };
}
export declare class PacketCheckJoinRes extends LocoBsonResponsePacket {
    Token: string;
    constructor(status: number, Token?: string);
    get PacketName(): string;
    readBodyJson(rawBody: any): void;
}
