import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
import { ChatType } from "../talk/chat/chat-type";
import { Long } from "bson";
import { ChatlogStruct } from "../talk/struct/chatlog-struct";
export declare class PacketForwardReq extends LocoBsonRequestPacket {
    MessageId: number;
    ChannelId: Long;
    Text: string;
    Type: ChatType;
    NoSeen: boolean;
    Extra: string;
    constructor(MessageId?: number, ChannelId?: Long, Text?: string, Type?: ChatType, NoSeen?: boolean, Extra?: string);
    get PacketName(): string;
    toBodyJson(): {
        chatId: Long;
        msgId: number;
        msg: string;
        type: ChatType;
        noSeen: boolean;
        extra: string;
    };
}
export declare class PacketForwardRes extends LocoBsonResponsePacket {
    Chatlog?: ChatlogStruct | undefined;
    constructor(status: number, Chatlog?: ChatlogStruct | undefined);
    get PacketName(): string;
    readBodyJson(rawBody: any): void;
}
