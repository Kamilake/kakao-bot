import { LocoBsonRequestPacket, LocoBsonResponsePacket } from "./loco-bson-packet";
import { Long } from "bson";
import { ChatType } from "../talk/chat/chat-type";
export declare class PacketShipReq extends LocoBsonRequestPacket {
    ChannelId: Long;
    Type: ChatType;
    Size: Long;
    Checksum: string;
    Ext: string;
    constructor(ChannelId?: Long, Type?: ChatType, Size?: Long, Checksum?: string, Ext?: string);
    get PacketName(): string;
    toBodyJson(): {
        c: Long;
        t: ChatType;
        s: Long;
        cs: string;
        e: string;
    };
}
export declare class PacketShipRes extends LocoBsonResponsePacket {
    Key: string;
    Host: string;
    Port: number;
    VHost: string;
    VHostV6: string;
    constructor(status: number, Key?: string, Host?: string, Port?: number, VHost?: string, VHostV6?: string);
    get PacketName(): string;
    readBodyJson(rawData: any): void;
}
