export declare class PromiseTicket<T> {
    private resolveList;
    private rejectList;
    constructor();
    createTicket(): Promise<T>;
    resolve(obj: T): void;
    reject(err: any): void;
    protected clear(): void;
}
