"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PromiseTicket = void 0;
class PromiseTicket {
    constructor() {
        this.resolveList = [];
        this.rejectList = [];
    }
    createTicket() {
        return new Promise((resolve, reject) => { this.resolveList.push(resolve); this.rejectList.push(reject); });
    }
    resolve(obj) {
        for (let resolve of this.resolveList) {
            resolve(obj);
        }
        this.clear();
    }
    reject(err) {
        for (let reject of this.rejectList) {
            reject(err);
        }
        this.clear();
    }
    clear() {
        this.resolveList = [];
        this.rejectList = [];
    }
}
exports.PromiseTicket = PromiseTicket;
//# sourceMappingURL=promise-ticket.js.map